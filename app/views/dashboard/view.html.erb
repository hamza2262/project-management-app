<nav class="navbar navbar-expand-lg navbar-light bg-white mt-2">
  <div class="container">
    <a class="navbar-brand" href="<%= user_session_path %>"><%= image_tag 'logo.png', alt: 'icon', class: 'logo-image' %></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="<%=root_path%>">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="<%=edit_user_registration_path%>">
            <% if user_signed_in? %>
              <p>Welcome,<%= current_user.name %></p>
            <% end %>
          </a>
        </li>
        <li class="nav-item">
          <%= image_tag(current_user.avatars.url(:thumb)) if current_user.avatars? %>
        </li>
        <li class="nav-item">
          <%= button_to "Sign Out", destroy_user_session_path, :class => "logout-button", :method => :delete %>
        </li>
      </ul>
    </div>
  </div>
</nav>
<section>
  <div class="container">
    <div class="row">
      <div class="col-3">
        <div class="card mt-5">
          <div class="card-header text-secondary">
          </div>
          <div class="card-body shadow-lg">
            <h5 class="card-title">Create Your Team</h5>
            <p class="card-text text-secondary">You can manage your projects more professionally by adding more members in your team</p>
            <%= button_to "New Team", new_team_path, method: :get, :class => "create-team-button"%>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>














